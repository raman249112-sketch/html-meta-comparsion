# HTML Meta Tag Comparison

This repository contains two HTML files:

- `index_with_meta.html`
- `index_without_meta.html`: No meta tag

Both look the same in the browser, but the meta tag ensures proper character encoding.
